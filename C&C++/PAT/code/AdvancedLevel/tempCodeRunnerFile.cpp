            // facs[count] = {i, 1};
            // n /= i;
            // while (n % i == 0)
            // {
            //     facs[count].count++;
            //     n /= i;
            // }
            // count++;